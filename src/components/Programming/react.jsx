import React, { memo } from "react";

import Navbar from "../Navbar";

const Reactjs = memo((props) => {
    return (
        <div>
            <Navbar />
            Reactjs
        </div>
    );
});

export default Reactjs;
