import React, { memo } from "react";

import Navbar from "../Navbar";

const JavaScript = memo((props) => {
    return (
        <div>
            <Navbar />
            JavaScript
        </div>
    );
});

export default JavaScript;
